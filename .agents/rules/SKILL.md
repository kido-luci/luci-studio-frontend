# Frontend Rules — see CLAUDE.md

The authoritative, code-verified guidance for this Astro blog frontend lives in
[`../../CLAUDE.md`](../../CLAUDE.md): commands, the per-change/verify loop,
architecture, the build-time Tailwind/PostCSS setup, the GSAP animation systems,
and Cloudflare Pages deployment.

> The previous contents of this file (an "Antigravity" rules doc describing a CDN
> Tailwind setup, Cloudflare **Workers** static deployment via `wrangler deploy`,
> a Vanta/Three.js/p5.js background, and an Outfit/Inter font stack) were **stale and
> contradicted the real project** on nearly every point. They were removed on
> 2026-06-24 to stop agents from following them. Keep this file a pointer — record
> project state in `CLAUDE.md` only, so there is a single source of truth.
