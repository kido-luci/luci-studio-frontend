---
// Thin route wrapper — shared body lives in LabPage so /lab (en) and
// /vi/lab render the same component, which reads the locale from the URL.
import LabPage from "../components/pages/LabPage.astro";
---
<LabPage />
