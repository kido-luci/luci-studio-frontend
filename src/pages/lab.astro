---
// Thin route wrapper — the body lives in LabPage so page logic stays out of the
// route file. English-only: only /blog is mirrored under /vi (see
// isBilingualPath in src/i18n).
import LabPage from "../components/pages/LabPage.astro";
---
<LabPage />
