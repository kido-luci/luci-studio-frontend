---
// Thin route wrapper — shared body lives in SeriesIndexPage so /blog/series (en) and
// /vi/blog/series render the same component, which reads the locale from the URL.
import SeriesIndexPage from "../../../components/pages/SeriesIndexPage.astro";
---
<SeriesIndexPage />
