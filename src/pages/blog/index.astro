---
// Thin route wrapper — shared body lives in BlogIndexPage so /blog (en) and
// /vi/blog render the same component, which reads the locale from the URL.
import BlogIndexPage from "../../components/pages/BlogIndexPage.astro";
---
<BlogIndexPage />
