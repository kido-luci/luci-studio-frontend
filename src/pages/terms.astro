---
// Thin route wrapper — shared body lives in TermsPage so /terms (en) and
// /vi/terms render the same component, which reads the locale from the URL.
import TermsPage from "../components/pages/TermsPage.astro";
---
<TermsPage />
