---
// Thin route wrapper — shared body lives in PortfolioPage so /portfolio (en) and
// /vi/portfolio render the same component, which reads the locale from the URL.
import PortfolioPage from "../components/pages/PortfolioPage.astro";
---
<PortfolioPage />
