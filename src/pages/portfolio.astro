---
// Thin route wrapper — the body lives in PortfolioPage so page logic stays out
// of the route file. English-only: only /blog is mirrored under /vi (see
// isBilingualPath in src/i18n).
import PortfolioPage from "../components/pages/PortfolioPage.astro";
---
<PortfolioPage />
