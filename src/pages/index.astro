---
import Layout from "../layouts/Layout.astro";
import ProjectCard from "../components/ProjectCard.astro";
import TimelineItem from "../components/TimelineItem.astro";
import { postService } from "../services/posts";
import { calculateReadTime, formatDate, slugify } from "../utils/blog";

const projects = [
  { title: 'memoirMe', year: '2024–Now', desc: 'Zero-knowledge RSA/AES encrypted diary with geolocation mapping, AI features, and cross-platform sync. Privacy-first, no server-side compromise.', links: [{ label: 'App Store', url: 'https://apps.apple.com/au/app/memoirme/id1640973202' }, { label: 'Play Store', url: 'https://play.google.com/store/apps/details?id=com.nartus.interactiveDiary&pcampaignid=web_share' }, { label: 'The Nartus', url: 'https://www.thenartus.com/' }], img: 'https://media.luci-studio.com/MEMOIRME.png', logo: 'https://media.luci-studio.com/MEMOIRME-LOGO.webp' },
  { title: 'TBchat', year: '2024–Now', desc: 'Web3 messenger with on-chain wallets, PoS miner orchestration, and audio chat with real-time waveforms. Cross-platform. No shortcuts.', links: [{ label: 'App Store', url: 'https://apps.apple.com/us/app/tbchat-web3/id6739870518' }, { label: 'Chain-Views', url: 'https://www.chainviews.net/' }], img: 'https://media.luci-studio.com/TBCHAT.png', logo: 'https://media.luci-studio.com/TBCHAT-LOGO.webp' },
  { title: 'IU PETSHOP', year: '2021–2022', desc: 'Blockchain-based pet microchip identification with VNPay payment integration. University capstone — designed the UI, built the app, shipped the demo.', links: [{ label: 'GitHub', url: 'https://github.com/binhtt13122000/PetCare_User_Mobile' }, { label: 'Demo Video', url: 'https://drive.google.com/file/d/1tYOotBwaK2GCclhNcoFpm_q9G9nu00Rf/view' }], img: 'https://media.luci-studio.com/IUPET-shop.png', logo: 'https://media.luci-studio.com/IU-PET-LOGO.png' },
  { title: 'Survive in Shadow', year: '2018', desc: 'Top-down 2D survival game built in Unity — procedural spawning, custom shadow system, resource management.', links: [{ label: 'itch.io', url: 'https://lapl1412.itch.io/survive-in-shadow' }], img: 'https://media.luci-studio.com/SURVIVE-GAME.png' },
];

const experience = [
  { id: 'TBchat', y: '2024–Now', role: 'Frontend Flutter Developer', co: 'TBchat · Nexsoft', loc: 'Vietnam & Hong Kong', desc: 'Building a blockchain messaging platform across Android, iOS, macOS, Windows.', details: ['Engineered foundational deep-linking architecture for cross-module content sharing.', 'Implemented real-time location sharing using WebSockets, Isolates, and background tracking.', 'Developed audio chat with custom waveforms and secure on-device speech-to-text transcription.', 'Built crypto wallet functionality with gas fee monitoring and offline L1 transaction signing.', 'Architected multi-environment configurations using Flutter Flavors (Dev, Staging, Prod).'], tech: ['Flutter', 'BLoC', 'ObjectBox', 'WebSocket', 'Go Router', 'Freezed'], img: 'https://media.luci-studio.com/TBCHAT.png', logo: 'https://media.luci-studio.com/TBCHAT-LOGO.webp', links: [{ label: 'App Store', url: 'https://apps.apple.com/us/app/tbchat-web3/id6739870518' }, { label: 'Chain-Views', url: 'https://www.chainviews.net/' }] },
  { id: 'memoirMe', y: '2024–Now', role: 'Full Stack Flutter Developer', co: 'memoirMe · The Nartus', loc: 'Australia', desc: 'Architected an interactive E2E encrypted diary application with enterprise-grade security.', details: ['Architected zero-knowledge E2E encryption using RSA and AES cryptographic algorithms.', 'Engineered subscription management with In-App Purchases (IAP) to drive monetization.', 'Built resilient media management with resumable background upload/download capabilities.', 'Integrated Firebase Analytics & Crashlytics for robust production health monitoring.', 'Developed local auto-save and offline-first data synchronization logic.'], tech: ['Flutter', 'Firebase', 'BLoC', 'RSA/AES', 'IAP', 'Crashlytics'], img: 'https://media.luci-studio.com/MEMOIRME.png', logo: 'https://media.luci-studio.com/MEMOIRME-LOGO.webp', links: [{ label: 'The Nartus', url: 'https://www.thenartus.com/' }] },
  { id: 'QuanhTa', y: '2022–2024', role: 'Full Stack Flutter Developer', co: 'QuanhTa · Vidieu', loc: 'Vietnam', desc: 'Real estate marketplace with advanced map-based search and automated data processing.', details: ['Engineered custom Google Maps integration with polygon-based area selection and filters.', 'Implemented real-time chat facilitating buyer-seller communication using gRPC.', 'Optimized Flutter Web performance and resolved Safari-specific CanvasKit renderer issues.', 'Built automated web scraping pipeline using Python, Selenium, and Scrapy.', 'Established complete CD pipeline for Flutter apps using Jenkins and Docker.'], tech: ['Flutter', 'Riverpod', 'Isar', 'gRPC', 'Python', 'Scrapy', 'Jenkins', 'Docker'], img: 'https://media.luci-studio.com/1aaaa6b7-b124-449f-ad63-8387dc5f4d89.png', links: [{ label: 'Web App', url: 'https://quanhta.vn/map/land' }, { label: 'App Store', url: 'https://apps.apple.com/vn/app/quanh-ta/id6453535963' }, { label: 'Play Store', url: 'https://play.google.com/store/apps/details?id=vn.quanhta.superapp&hl=en' }] },
  { id: 'IU PETSHOP', y: '2021–2022', role: 'Flutter Developer & UI/UX Designer', co: 'IU PETSHOP · FPT University', loc: 'Captain Project', desc: 'Pet care ecosystem with blockchain identification and secure payment processing.', details: ['Implemented blockchain-based microchip identification system for pet tracking.', 'Integrated VNPay payment gateway through WebView for sandbox testing environment.', 'Designed complete mobile interface using Adobe XD and custom Flutter animations.', 'Integrated Firebase OAuth for secure user authentication across the platform.'], tech: ['Flutter', 'GetX', 'Hasura GraphQL', 'VNPay', 'Firebase Auth', 'Adobe XD'], img: 'https://media.luci-studio.com/IUPET-shop.png', logo: 'https://media.luci-studio.com/IU-PET-LOGO.png', links: [{ label: 'GitHub', url: 'https://github.com/binhtt13122000/PetCare_User_Mobile' }, { label: 'Demo Video', url: 'https://drive.google.com/file/d/1tYOotBwaK2GCclhNcoFpm_q9G9nu00Rf/view' }] },
];

const indieProjects = [
  { id: 'Luci Studio', y: '2026', role: 'Founder & Full Stack Developer', co: 'Luci Studio', loc: 'Vietnam', desc: 'Built the high-performance web portfolio and technical blog you are currently viewing.', details: ['Designed a premium, motion-rich UX/UI with custom-built interactive components.', 'Engineered for maximum performance (95+ Lighthouse score) using Astro static generation.', 'Implemented complex responsive layouts and magnetic hover interactions.', 'Deployed a global-first infrastructure using Cloudflare Pages edge delivery.'], tech: ['Astro', 'Go', 'PostgreSQL', 'Cloudflare Pages', 'Fly.io', 'Cloudflare R2', 'Tailwind CSS'], img: 'https://media.luci-studio.com/LUCI-STUDIO.png', logo: '/images/logo_icon.png', links: [{ label: 'Live Site', url: 'https://luci-studio.com/' }] },
  { id: 'UwU EBOOK', y: '2024', role: 'Full Stack Developer', co: 'UwU EBOOK', loc: 'Owner Project', desc: 'Digital publishing platform for Vietnamese content with full-stack automation.', details: ['Architected microservices backend using Golang and the Ent framework.', 'Built complex data crawler system with Python and Scrapy for automated content gathering.', 'Deployed containerized infrastructure using Docker for seamless scalability.'], tech: ['Go', 'Ent', 'MySQL', 'Python', 'Scrapy', 'Docker'], img: 'https://images.unsplash.com/photo-1555066931-4365d14bab8c?auto=format&fit=crop&q=80&w=800', links: [] },
  { id: 'Survive in Shadow', y: '2018', role: 'Game Developer', co: 'Survive in Shadow', loc: 'Independent Project', desc: 'A minimalist 2D top-down survival game built with Unity.', details: ['Designed core survival mechanics including resource management and procedural enemy spawning.', 'Implemented custom 2D lighting system to enhance atmospheric tension.', 'Developed responsive player controls and inventory management systems.'], tech: ['Unity', 'C#', '2D Art'], img: 'https://media.luci-studio.com/SURVIVE-GAME.png', links: [{ label: 'itch.io', url: 'https://lapl1412.itch.io/survive-in-shadow' }] },
];

const rawPosts = await postService.getAll();
const blogPosts = rawPosts
  .filter(p => !p.topics?.some(t => t.toLowerCase().includes('art')))
  .map(p => ({
  id: p.id,
  title: p.title,
  subtitle: p.subtitle || '',
  date: formatDate(p.created_at),
  read: calculateReadTime(p.content),
  tag: (p.topics && p.topics.length > 0) ? p.topics[0] : 'Engineering',
  img: p.cover_image_url || 'https://images.unsplash.com/photo-1555066931-4365d14bab8c?auto=format&fit=crop&q=80&w=800',
  likes: p.likes ?? 0,
}));
const API_URL = import.meta.env.PUBLIC_API_URL || '';

const artItems: { img: string; label: string; year: string; date: string; topic: string; url?: string; id?: number; likes?: number }[] =
  rawPosts.some(p => p.topics?.some(t => t.toLowerCase().includes('art')))
    ? rawPosts
        .filter(p => p.topics?.some(t => t.toLowerCase().includes('art')))
        .map(p => ({
          img: p.cover_image_url || 'https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?auto=format&fit=crop&q=80&w=800',
          label: p.title,
          year: new Date(p.created_at).getFullYear().toString(),
          date: formatDate(p.created_at),
          topic: (p.topics || ['Art']).join(' · '),
          url: `/blog/${slugify(p.title)}-${p.id}`,
          id: p.id,
          likes: p.likes ?? 0,
        }))
    : [
        { img: 'https://images.unsplash.com/photo-1633356122544-f134324a6cee?auto=format&fit=crop&q=80&w=1200', label: 'Neon Currents', year: '2024', date: 'Jan 2024', topic: 'Digital Art' },
        { img: 'https://images.unsplash.com/photo-1618005182384-a83a8bd57fbe?auto=format&fit=crop&q=80&w=800', label: 'Deep Space', year: '2024', date: 'Mar 2024', topic: 'Digital Art' },
        { img: 'https://images.unsplash.com/photo-1620641788421-7a1c342ea42e?auto=format&fit=crop&q=80&w=800', label: 'Bloom', year: '2024', date: 'Jun 2024', topic: 'Illustration' },
        { img: 'https://images.unsplash.com/photo-1614850523296-d8c1af93d400?auto=format&fit=crop&q=80&w=800', label: 'Ember', year: '2023', date: 'Nov 2023', topic: 'Digital Art' },
        { img: 'https://images.unsplash.com/photo-1550684848-fac1c5b4e853?auto=format&fit=crop&q=80&w=800', label: 'Circuit', year: '2023', date: 'Aug 2023', topic: 'Digital Art' },
      ];
---

<Layout title="Luci Studio | Tech Blog, Digital Art & Innovative Apps">

  <!-- NAV -->
  <nav class="fixed top-0 w-full z-[100] border-b border-white/5" style="background:var(--nav-bg);backdrop-filter:blur(16px)">
    <div class="max-w-7xl mx-auto px-4 md:px-6 h-20 flex items-center justify-between">
      <a href="/" class="flex items-center gap-3 group">
        <img src="/images/logo_icon.png" alt="Luci Studio Logo" class="h-9 w-9 rounded-xl shadow-lg group-hover:shadow-violet-500/50 transition-all duration-300" />
        <span class="text-xl font-black tracking-[0em]">LUCI <span class="text-shimmer">STUDIO</span><span class="text-violet-500">.</span></span>
      </a>
      <div class="hidden md:flex items-center gap-8">
        {['About','Work','Art','Blog'].map(l => (
          <a href={`#${l.toLowerCase()}`} class="text-xs font-bold text-gray-400 hover:text-white transition-all duration-300 uppercase tracking-[0.2em]">{l}</a>
        ))}
        <a href="https://medium.com/@lapl1412.work" target="_blank" class="text-xs font-bold text-gray-400 hover:text-white transition-all duration-300 uppercase tracking-[0.2em]">Medium</a>
        <a href="https://www.linkedin.com/in/trung-l%E1%BA%ADp-luci-ba0b6a339/" target="_blank" class="text-xs font-bold text-gray-400 hover:text-white transition-all duration-300 uppercase tracking-[0.2em]">LinkedIn</a>
      </div>

      <div class="flex items-center gap-4">
        <!-- Theme Toggle Button -->
        <button 
          id="theme-toggle" 
          onclick="window.toggleTheme()"
          class="w-10 h-10 flex items-center justify-center rounded-full border border-white/10 hover:border-violet-500/40 hover:bg-white/5 transition-all duration-300 group"
          aria-label="Toggle theme"
        >
          <svg class="w-5 h-5 text-gray-400 group-hover:text-white transition-colors dark-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"></path>
          </svg>
          <svg class="w-5 h-5 text-gray-400 group-hover:text-white transition-colors light-icon hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"></path>
          </svg>
        </button>

        <a href="#contact" class="magnetic hidden sm:block px-5 py-2.5 bg-white text-black text-xs font-black rounded-full hover:bg-violet-500 hover:text-white transition-all duration-300 uppercase tracking-widest">Let's Talk</a>
        
        <!-- Mobile Menu Toggle -->
        <button id="mobile-menu-toggle" class="md:hidden w-10 h-10 flex flex-col items-center justify-center gap-1.5 z-[100] group">
          <span class="w-6 h-0.5 transition-transform duration-300 group-[.active]:rotate-45 group-[.active]:translate-y-2" style="background-color: var(--text-primary);"></span>
          <span class="w-6 h-0.5 transition-opacity duration-300 group-[.active]:opacity-0" style="background-color: var(--text-primary);"></span>
          <span class="w-6 h-0.5 transition-transform duration-300 group-[.active]:-rotate-45 group-[.active]:-translate-y-2" style="background-color: var(--text-primary);"></span>
        </button>
      </div>
    </div>
  </nav>

  <!-- Mobile Menu Overlay -->
  <div id="mobile-menu" class="fixed inset-0 bg-black/95 backdrop-blur-3xl z-[90] flex flex-col items-center justify-center gap-8 opacity-0 pointer-events-none transition-all duration-500">
    {['About','Work','Art','Blog'].map(l => (
      <a href={`#${l.toLowerCase()}`} class="mobile-nav-link text-4xl font-black tracking-tighter hover:text-violet-500 transition-colors">{l}</a>
    ))}
    <div class="h-px w-12 bg-white/10 my-4"></div>
    <a href="https://medium.com/@lapl1412.work" target="_blank" class="mobile-nav-link text-xl font-bold text-gray-400 uppercase tracking-widest">Medium</a>
    <a href="https://www.linkedin.com/in/trung-l%E1%BA%ADp-luci-ba0b6a339/" target="_blank" class="mobile-nav-link text-xl font-bold text-gray-400 uppercase tracking-widest">LinkedIn</a>
    <a href="#contact" class="mobile-nav-link mt-8 px-8 py-4 bg-white text-black font-black rounded-full uppercase tracking-widest">Let's Talk</a>
  </div>

  <main data-api-url={API_URL}>

    <!-- HERO -->
    <section class="relative min-h-screen flex flex-col justify-center items-center pt-20 px-4 md:px-6">
      <div class="hero-bg-img absolute inset-0 pointer-events-none z-[0]" style="background-size:cover;background-position:center;background-repeat:no-repeat; -webkit-mask-image: radial-gradient(circle at center, black 30%, transparent 100%);"></div>
      <!-- Vanta.js halo is handled in Layout.astro -->

      <!-- Floating orbs for depth -->
      <div class="hero-orb hero-orb-1" aria-hidden="true"></div>
      <div class="hero-orb hero-orb-2" aria-hidden="true"></div>

      <div class="relative z-10 text-center max-w-6xl mx-auto">



        <!-- Main Headline -->
        <h1 class="hero-headline" data-word-reveal>
          <!-- Line 1: LUCI STUDIO -->
          <span class="hero-line hero-line-top">
            <span class="reveal-word hero-word-main">
              <span class="text-white">LUCI</span>
            </span>
            <span class="reveal-word hero-word-accent">
              <span class="text-shimmer">STUDIO</span>
            </span>
          </span>

          <!-- Divider line -->
          <span class="hero-divider" aria-hidden="true">
            <span class="hero-divider-line"></span>
            <span class="hero-divider-diamond">◆</span>
            <span class="hero-divider-line"></span>
          </span>

          <!-- Line 2: BLOG • ART • APPS -->
          <span class="hero-line hero-line-bottom">
            <span class="reveal-word hero-word-sub">
              <span class="text-gray-400">TECH BLOG</span>
            </span>
            <span class="reveal-word hero-word-sub">
              <span class="text-gray-500 mx-2">•</span>
            </span>
            <span class="reveal-word hero-word-sub">
              <span class="text-gray-200  ">DIGITAL ART</span>
            </span>
            <span class="reveal-word hero-word-sub">
              <span class="text-gray-500 mx-2">•</span>
            </span>
            <span class="reveal-word hero-word-sub hero-word-last">
              <span class="text-cyan-400">APPS</span>
            </span>
          </span>
        </h1>

        <!-- Subtitle -->
        <p class="hero-subtitle" style="color: white !important; text-shadow: 0 2px 10px rgba(0,0,0,0.8), 0 0 20px rgba(0,0,0,0.5);">
          Engineering high-performance applications across mobile, desktop, and web. No shortcuts. Just rigorous architecture, deliberate design, and code that ships.
        </p>

        <!-- CTA Buttons -->
        <div class="flex flex-col sm:flex-row items-center justify-center gap-5">
          <a href="#work" class="magnetic group relative px-8 py-4 hero-cta-primary text-sm font-black rounded-full overflow-hidden hover:shadow-[0_0_60px_rgba(139,92,246,0.4)] transition-all duration-500 hover:scale-105">
            <span class="relative z-10 uppercase tracking-widest">View Work</span>
            <div class="absolute inset-0 bg-gradient-to-r from-violet-600 to-blue-600 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
            <span class="absolute inset-0 z-10 flex items-center justify-center text-white opacity-0 group-hover:opacity-100 font-black uppercase tracking-widest transition-opacity duration-500">View Work</span>
            <div class="absolute -inset-1 bg-gradient-to-r from-violet-600 to-blue-600 rounded-full opacity-0 group-hover:opacity-20 blur transition-opacity duration-500"></div>
          </a>
          <a href="#art" class="magnetic hero-cta-secondary px-8 py-4 border text-sm font-bold rounded-full transition-all duration-300 uppercase tracking-widest hover:scale-105">
            See Art →
          </a>
        </div>

        <!-- Quick stat pills -->
        <div class="hero-stats">
          <span class="hero-stat-pill">5+ yrs Flutter</span>
          <span class="hero-stat-sep">·</span>
          <span class="hero-stat-pill">Android · iOS · Web · macOS · Windows</span>
          <span class="hero-stat-sep">·</span>
          <span class="hero-stat-pill">Go · Web3 · On-device AI</span>
        </div>
      </div>

      <!-- Scroll hint -->
      <div class="absolute bottom-10 left-1/2 -translate-x-1/2 flex flex-col items-center gap-2 opacity-30">
        <span class="text-xs uppercase tracking-widest">Scroll</span>
        <div class="w-px h-16 bg-gradient-to-b from-white to-transparent" style="animation:float 2s ease-in-out infinite"></div>
      </div>
    </section>

    <!-- MARQUEE STRIP -->
    <div class="relative py-6 border-y border-white/5 bg-black/40 overflow-hidden">
      <div class="flex whitespace-nowrap animate-marquee">
        {['Flutter','Dart','BLoC','Riverpod','Firebase','Go Router','Freezed','Retrofit','gRPC','WebSocket','Docker','GitHub Actions','ObjectBox','GCP','Clean Architecture','Flutter Test','Mocktail','Figma','Rive','Custom Paint'].flatMap(t => [
          <span class="mx-8 text-xs font-black uppercase tracking-[0.3em] text-gray-400">{t}</span>,
          <span class="mx-4 text-gray-700 text-lg">✦</span>
        ])}
        {['Flutter','Dart','BLoC','Riverpod','Firebase','Go Router','Freezed','Retrofit','gRPC','WebSocket','Docker','GitHub Actions','ObjectBox','GCP','Clean Architecture','Flutter Test','Mocktail','Figma','Rive','Custom Paint'].flatMap(t => [
          <span class="mx-8 text-xs font-black uppercase tracking-[0.3em] text-gray-400">{t}</span>,
          <span class="mx-4 text-gray-700 text-lg">✦</span>
        ])}
      </div>
    </div>


    <!-- CONTENT WRAPPER FOR SMOOTH TRANSITIONS -->
    <div class="relative bg-black/60 border-t border-white/5 light-mode:bg-white/80" style="background-color: var(--bg-secondary); border-color: var(--border-color);">
      
            <!-- BLOG -->
      <section id="blog" class="py-16 px-4 md:px-6">
        <div class="max-w-7xl mx-auto">
          <div data-reveal class="mb-20 text-center">
            <span class="text-xs font-black text-violet-500 uppercase tracking-[0.3em] mb-3 block">Raw Output</span>
            <h2 data-ml9 class="text-6xl md:text-8xl font-black tracking-tighter">THOUGHTS<span class="text-violet-500">.</span></h2>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            {blogPosts.map((p, i) => (
              <div class="relative" data-post-id={p.id} data-post-likes={p.likes}>
                <a href={`/blog/${slugify(p.title)}-${p.id}`} class="block no-underline">
                  <article class="tilt-card blog-card group cursor-pointer p-5 sm:p-6 rounded-2xl border border-white/5 transition-all duration-500 bg-white/[0.02]" data-blog-card={String(i)}>
                    <div class="blog-img-container aspect-video rounded-2xl overflow-hidden mb-6 border border-white/5 group-hover:border-violet-500/20 transition-all duration-500">
                      <img src={p.img} alt={p.title} class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700" onerror="this.style.opacity='0'" />
                    </div>
                    <div class="flex items-center gap-3 mb-4">
                      <span class="text-xs font-black text-violet-400 uppercase tracking-widest">{p.tag}</span>
                      <span class="w-1 h-1 bg-gray-700 rounded-full"></span>
                      <span class="text-xs text-gray-600">{p.date}</span>
                      <span class="w-1 h-1 bg-gray-700 rounded-full"></span>
                      <span class="text-xs text-gray-600">{p.read} read</span>
                    </div>
                    <h3 class="text-xl font-bold leading-snug group-hover:text-violet-400 transition-colors duration-300 line-clamp-2">{p.title}</h3>
                    {p.subtitle && <p class="text-sm text-gray-500 mt-2 leading-relaxed line-clamp-3">{p.subtitle}</p>}
                    <div class="mt-4 pt-4 border-t border-white/5 flex items-center justify-between">
                      <span class="text-xs text-gray-600">Read →</span>
                      <span class="tile-like-area flex items-center gap-1.5 cursor-pointer group/like" data-id={p.id}>
                        <svg class="tile-like-icon w-4 h-4 text-gray-500 group-hover/like:text-rose-400 transition-colors duration-200" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                          <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
                        </svg>
                        <span class="tile-like-count text-xs text-gray-500 group-hover/like:text-rose-400 transition-colors duration-200">{p.likes}</span>
                      </span>
                    </div>
                  </article>
                </a>
              </div>
            ))}
          </div>
        </div>
      </section>

      <div class="flex flex-col items-center py-8 opacity-40">
        <div class="w-0.5 h-12 bg-gradient-to-b from-pink-500 to-transparent"></div>
      </div>

      <!-- WORK -->
      <section id="work" class="py-16 px-4 md:px-6">
        <div class="max-w-7xl mx-auto">
          <div data-reveal class="mb-20">
            <span class="text-xs font-black text-violet-500 uppercase tracking-[0.3em] mb-3 block">What I Built</span>
            <h2 data-ml9 class="text-6xl md:text-8xl font-black tracking-tighter">PORTFOLIO<span class="text-violet-500">.</span></h2>
            <div data-reveal class="line-draw h-px bg-gradient-to-r from-violet-500 to-transparent mt-6 max-w-xs"></div>
          </div>
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            {projects.map((p, i) => (
              <ProjectCard {...p} index={i} />
            ))}
          </div>
        </div>
      </section>

      <div class="flex flex-col items-center py-8 opacity-40">
        <div class="w-0.5 h-12 bg-gradient-to-b from-pink-500 to-transparent"></div>
      </div>

      <!-- ART GALLERY -->
      <section id="art" class="py-16 px-4 md:px-6 relative">
        <div class="absolute inset-0 pointer-events-none" style="clip-path: inset(0);">
          <div id="art-bg" class="sticky top-0 w-full h-screen" style="background-image:url('https://media.luci-studio.com/ChatGPT%20Image%20May%203%2C%202026%2C%2009_10_36%20AM.png');background-size:cover;background-position:center;background-repeat:no-repeat"></div>
        </div>
        <div class="relative max-w-7xl mx-auto">
          <div data-reveal class="mb-20 flex flex-col md:flex-row md:items-end justify-between gap-8">
            <div>
              <span class="text-xs font-black uppercase tracking-[0.3em] mb-3 block" style="background:linear-gradient(90deg,#f472b6,#a78bfa,#60a5fa);-webkit-background-clip:text;-webkit-text-fill-color:transparent;background-clip:text">Off-Clock</span>
              <h2 data-ml9 class="text-6xl md:text-8xl font-black tracking-tighter" style="color:white">THE ART<span style="color:#60a5fa">.</span></h2>
            </div>
            <p class="text-white/50 max-w-xs text-sm leading-relaxed">Rigorous aesthetic discipline. Where raw engineering logic meets deliberate visual hierarchy.</p>
          </div>

          <div class="masonry-art-grid">
            {artItems.map((a) => {
              const infoBar = (
                <div class="px-3 py-2 flex items-start justify-between gap-2 rounded-xl" style="background:rgba(10,10,20,0.85)">
                  <div class="flex-1 min-w-0">
                    <p class="font-bold text-sm leading-snug line-clamp-2" style="color:#fff">{a.label}</p>
                    <p class="text-[10px] uppercase tracking-widest mt-0.5 truncate" style="color:#93c5fd">{a.topic} · {a.date}</p>
                  </div>
                  {a.id != null && (
                    <span
                      class="art-like-area flex items-center gap-1 cursor-pointer flex-shrink-0 group/like mt-0.5"
                      data-id={String(a.id)}
                      data-likes={String(a.likes ?? 0)}
                    >
                      <svg class="art-like-icon w-3.5 h-3.5 group-hover/like:text-rose-400 transition-colors duration-200" style="color:#cbd5e1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                        <path d="M20.84 4.61a5.5 5.5 0 0 0-7.78 0L12 5.67l-1.06-1.06a5.5 5.5 0 0 0-7.78 7.78l1.06 1.06L12 21.23l7.78-7.78 1.06-1.06a5.5 5.5 0 0 0 0-7.78z"/>
                      </svg>
                      <span class="art-like-count text-[10px] group-hover/like:text-rose-400 transition-colors duration-200" style="color:#cbd5e1">{a.likes ?? 0}</span>
                    </span>
                  )}
                </div>
              );
              return a.url ? (
                <a href={a.url} class="masonry-art-item group block" data-art-card>
                  <div class="art-img-frame relative rounded-2xl overflow-hidden border-4 border-white/40 hover:border-purple-400/80 transition-all duration-500">
                    <img src={a.img} alt={a.label} class="w-full h-auto block transition-transform duration-700 group-hover:scale-105" loading="lazy" />
                    <div class="absolute inset-x-0 bottom-0 h-1/3 bg-gradient-to-t from-black/70 to-transparent pointer-events-none"></div>
                  </div>
                  {infoBar}
                </a>
              ) : (
                <div class="masonry-art-item group" data-art-card>
                  <div class="art-img-frame relative rounded-2xl overflow-hidden border-4 border-white/40 hover:border-purple-400/80 transition-all duration-500">
                    <img src={a.img} alt={a.label} class="w-full h-auto block transition-transform duration-700 group-hover:scale-105" loading="lazy" />
                    <div class="absolute inset-x-0 bottom-0 h-1/3 bg-gradient-to-t from-black/70 to-transparent pointer-events-none"></div>
                  </div>
                  {infoBar}
                </div>
              );
            })}
          </div>
        </div>
      </section>

      <div class="flex flex-col items-center py-8 opacity-40">
        <div class="w-0.5 h-12 bg-gradient-to-b from-pink-500 to-transparent"></div>
      </div>

<!-- ABOUT ME -->
      <section id="about" class="py-24 px-4 md:px-6 overflow-hidden">
        <div class="max-w-7xl mx-auto">
          <div class="grid grid-cols-1 lg:grid-cols-2 gap-16 items-center">
            <!-- Left: Artistic Image / Visual -->
            <div data-reveal="left" class="relative group">
              <div class="absolute -inset-4 bg-gradient-to-tr from-violet-600/20 to-blue-600/20 rounded-2xl blur-2xl opacity-50 group-hover:opacity-100 transition-opacity duration-700"></div>
              <div class="relative aspect-square md:aspect-auto md:h-[600px] rounded-2xl overflow-hidden border border-white/10 bg-white/[0.02]">
                <img src="https://media.luci-studio.com/My%20Story.webp" alt="Trung Lập" class="w-full h-full object-cover group-hover:scale-105 transition-all duration-700" />
                <!-- Overlay details -->
                <div class="absolute bottom-8 left-8 right-8 p-6 backdrop-blur-xl bg-black/40 border border-white/10 rounded-2xl">
                  <p class="text-xs font-black text-violet-400 uppercase tracking-[0.2em] mb-1">Based in</p>
                  <p class="text-xl font-bold">Ho Chi Minh City, VN</p>
                </div>
              </div>
            </div>

            <!-- Right: Content -->
            <div data-reveal="right">
              <span class="text-xs font-black text-violet-500 uppercase tracking-[0.3em] mb-4 block">The Person</span>
              <h2 data-ml9 class="text-5xl md:text-7xl font-black tracking-tighter mb-8">MY STORY<span class="text-violet-500">.</span></h2>
              
              <div class="space-y-6 text-gray-400 text-lg leading-relaxed">
                <p>
                  I'm <span class="text-white font-bold">Trung Lập (Luci)</span>. I engineer high-performance systems. Over five years specializing in Flutter, I've shipped production applications across <span class="text-white font-bold">Android, iOS, macOS, Windows, and Web</span>, backed by robust Go microservices and automated Python pipelines.
                </p>
                <p>
                  I operate across the entire product lifecycle. Architecture. State management. Real-time infrastructure. CI/CD. I build systems that scale, from a <span class="text-white">zero-knowledge encrypted diary</span> to a live Web3 messaging platform with integrated on-chain wallets.
                </p>
                <p>
                  I am also a visual artist. That discipline strictly dictates how I approach <span class="text-white">visual hierarchy, proportion, and interface logic</span>. My design decisions are deliberate. Nothing is accidental.
                </p>
                <p>
                  My baseline standard is production-grade stability. No fragility. No deferred technical debt. I execute with <span class="text-shimmer font-bold">clean architecture</span>, rigid state management, and an absolute obsession with edge cases. Ship. Refine. Repeat.
                </p>
              </div>

              <!-- Quick Facts Grid -->
              <div class="grid grid-cols-2 gap-4 mt-12">
                {[
                  { label: 'Origin story', val: 'Drawing · then Game dev · 2018' },
                  { label: 'Stack', val: 'Flutter · Go · PostgreSQL' },
                  { label: 'Art mediums', val: 'Pencil · Ballpoint · Watercolor · Digital' },
                  { label: 'Motto', val: 'Ship. Refine. Repeat.' }
                ].map((f, fi) => (
                  <div data-reveal="scale" data-delay={String(fi + 1)} class="p-4 rounded-2xl border border-white/5 bg-white/[0.02] hover:bg-white/[0.05] transition-colors">
                    <p class="text-[10px] font-black text-gray-600 uppercase tracking-widest mb-1">{f.label}</p>
                    <p class="text-sm font-bold text-gray-300">{f.val}</p>
                  </div>
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>

      <div class="flex flex-col items-center py-8 opacity-40">
        <div class="w-0.5 h-12 bg-gradient-to-b from-pink-500 to-transparent"></div>
      </div>

      <!-- STATS -->
      <section class="py-6 px-4 md:px-6">
        <div class="max-w-5xl mx-auto grid grid-cols-2 md:grid-cols-4 gap-1">
          {[
            { n: 5, suffix: '+', label: 'Years', sub: 'Deep in Flutter' },
            { n: 10, suffix: '+', label: 'Projects', sub: 'In production. For real.' },
            { n: 5, suffix: '', label: 'Platforms', sub: 'Android · iOS · macOS · Web · Windows' },
            { n: 50, suffix: '%', label: 'Scholarship', sub: 'FPT University · Merit' },
          ].map((s, i) => (
            <div data-reveal data-delay={String(i+1)} class="tilt-card p-5 sm:p-8 border border-white/5 rounded-2xl text-center hover:border-violet-500/20 transition-all duration-500 bg-white/[0.02]">
              <div class="text-6xl md:text-7xl font-black tracking-tighter text-transparent bg-clip-text bg-gradient-to-br from-white to-gray-500 mb-2">
                <span class="count-up" data-count={s.n} data-suffix={s.suffix}>0{s.suffix}</span>
              </div>
              <div class="text-white font-bold mb-1">{s.label}</div>
              <div class="text-xs text-gray-400 uppercase tracking-widest">{s.sub}</div>
            </div>
          ))}
        </div>
      </section>
      <div class="flex flex-col items-center py-6 opacity-40">
        <div class="w-0.5 h-12 bg-gradient-to-b from-pink-500 to-transparent"></div>
      </div>

      <!-- EXPERIENCE -->
      <section id="experience" class="py-16 px-4 md:px-6">
        <div class="max-w-7xl mx-auto">
          <div data-reveal class="mb-20">
            <span class="text-xs font-black text-pink-500 uppercase tracking-[0.3em] mb-3 block">The Record</span>
            <h2 data-ml9 class="text-6xl md:text-8xl font-black tracking-tighter">EXPERIENCE<span class="text-pink-500">.</span></h2>
          </div>

          <div class="mb-12">
            <h3 data-reveal class="text-xs font-black text-gray-500 uppercase tracking-[0.3em] mb-8 flex items-center gap-4">
              Paid Work
              <div class="h-px flex-1 bg-white/5"></div>
            </h3>
            <div class="space-y-4 timeline-container">
              {experience.map((j, i) => (
                <TimelineItem {...j} index={i} />
              ))}
            </div>
          </div>

          <div>
            <h3 data-reveal class="text-xs font-black text-gray-500 uppercase tracking-[0.3em] mb-8 flex items-center gap-4">
              Own Bets
              <div class="h-px flex-1 bg-white/5"></div>
            </h3>
            <div class="space-y-4 timeline-container">
              {indieProjects.map((j, i) => (
                <TimelineItem {...j} index={i} />
              ))}
            </div>
          </div>
        </div>
      </section>

      <div class="flex flex-col items-center py-8 opacity-40">
        <div class="w-0.5 h-12 bg-gradient-to-b from-pink-500 to-transparent"></div>
      </div>

      <!-- SKILLS -->
      <section id="skills" class="py-16 px-4 md:px-6">
        <div class="max-w-7xl mx-auto">
          <div data-reveal class="mb-20">
            <span class="text-xs font-black text-emerald-500 uppercase tracking-[0.3em] mb-3 block">The Stack</span>
            <h2 data-ml9 class="text-6xl md:text-8xl font-black tracking-tighter">SKILLS<span class="text-emerald-500">.</span></h2>
          </div>
          
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-4">
            <!-- Mobile Card -->
            <div data-reveal class="group p-5 sm:p-8 rounded-2xl border border-white/5 bg-white/[0.02] hover:border-violet-500/20 transition-all duration-500 relative overflow-hidden">
               <div class="absolute -right-4 -top-4 w-24 h-24 bg-violet-500/10 rounded-full blur-2xl group-hover:bg-violet-500/20 transition-all"></div>
               <h3 class="text-xs font-black text-violet-500 uppercase tracking-[0.3em] mb-8 flex items-center gap-2">
                 <span class="w-2 h-2 rounded-full bg-violet-500"></span>
                 Mobile
               </h3>
               <div class="flex flex-wrap gap-2">
                 {['Flutter','Dart','BLoC','Riverpod','Clean Architecture','Custom UI','Animations','ObjectBox'].map(s => (
                   <span class="px-3 py-2 bg-white/5 border border-white/10 rounded-xl text-[11px] font-bold text-gray-400 hover:text-white hover:border-violet-500/40 hover:bg-violet-500/10 transition-all cursor-default">{s}</span>
                 ))}
               </div>
            </div>

            <!-- Backend Card -->
            <div data-reveal data-delay="1" class="group p-8 rounded-2xl border border-white/5 bg-white/[0.02] hover:border-blue-500/20 transition-all duration-500 relative overflow-hidden">
               <div class="absolute -right-4 -top-4 w-24 h-24 bg-blue-500/10 rounded-full blur-2xl group-hover:bg-blue-500/20 transition-all"></div>
               <h3 class="text-xs font-black text-blue-500 uppercase tracking-[0.3em] mb-8 flex items-center gap-2">
                 <span class="w-2 h-2 rounded-full bg-blue-500"></span>
                 Backend
               </h3>
               <div class="flex flex-wrap gap-2">
                 {['Golang','gRPC','REST API','Firebase','GCP','PostgreSQL','MySQL','Redis'].map(s => (
                   <span class="px-3 py-2 bg-white/5 border border-white/10 rounded-xl text-[11px] font-bold text-gray-400 hover:text-white hover:border-blue-500/40 hover:bg-blue-500/10 transition-all cursor-default">{s}</span>
                 ))}
               </div>
            </div>

            <!-- DevOps Card -->
            <div data-reveal data-delay="2" class="group p-8 rounded-2xl border border-white/5 bg-white/[0.02] hover:border-cyan-500/20 transition-all duration-500 relative overflow-hidden">
               <div class="absolute -right-4 -top-4 w-24 h-24 bg-cyan-500/10 rounded-full blur-2xl group-hover:bg-cyan-500/20 transition-all"></div>
               <h3 class="text-xs font-black text-cyan-500 uppercase tracking-[0.3em] mb-8 flex items-center gap-2">
                 <span class="w-2 h-2 rounded-full bg-cyan-500"></span>
                 DevOps
               </h3>
               <div class="flex flex-wrap gap-2">
                 {['Docker','CI/CD','GitHub Actions','Jenkins','Cloudflare','Docker Compose','Linux'].map(s => (
                   <span class="px-3 py-2 bg-white/5 border border-white/10 rounded-xl text-[11px] font-bold text-gray-400 hover:text-white hover:border-cyan-500/40 hover:bg-cyan-500/10 transition-all cursor-default">{s}</span>
                 ))}
               </div>
            </div>

            <!-- Security & AI Card -->
            <div data-reveal data-delay="3" class="group p-8 rounded-2xl border border-white/5 bg-white/[0.02] hover:border-pink-500/20 transition-all duration-500 relative overflow-hidden">
               <div class="absolute -right-4 -top-4 w-24 h-24 bg-pink-500/10 rounded-full blur-2xl group-hover:bg-pink-500/20 transition-all"></div>
               <h3 class="text-xs font-black text-pink-500 uppercase tracking-[0.3em] mb-8 flex items-center gap-2">
                 <span class="w-2 h-2 rounded-full bg-pink-500"></span>
                 Specialized
               </h3>
               <div class="flex flex-wrap gap-2">
                 {['RSA/AES','Web3','Blockchain','Local LLM','llama.cpp','On-device AI','Security'].map(s => (
                   <span class="px-3 py-2 bg-white/5 border border-white/10 rounded-xl text-[11px] font-bold text-gray-400 hover:text-white hover:border-pink-500/40 hover:bg-pink-500/10 transition-all cursor-default">{s}</span>
                 ))}
               </div>
            </div>

            <!-- Art & Creative Card (full-width) -->
            <div data-reveal data-delay="4" class="group p-8 rounded-2xl border border-white/5 bg-white/[0.02] hover:border-amber-500/20 transition-all duration-500 relative overflow-hidden md:col-span-2 lg:col-span-4">
               <div class="absolute -right-8 -top-8 w-48 h-48 bg-amber-500/8 rounded-full blur-3xl group-hover:bg-amber-500/15 transition-all"></div>
               <div class="flex flex-col md:flex-row md:items-start gap-8">
                 <div class="flex-shrink-0 md:w-56">
                   <h3 class="text-xs font-black text-amber-500 uppercase tracking-[0.3em] mb-4 flex items-center gap-2">
                     <span class="w-2 h-2 rounded-full bg-amber-500"></span>
                     Art & Creative
                   </h3>
                   <p class="text-gray-600 text-sm leading-relaxed">Traditional and digital — portraits, character illustration, watercolor, and pixel art. A creative practice running parallel to the engineering work since 2016.</p>
                 </div>
                 <div class="flex flex-wrap gap-2 content-start">
                   {['Pencil Portrait','Ballpoint Pen','Anime · Manga','Watercolor','Digital Illustration','Pixel Art','Character Design','Ink & Sketch','Figure Drawing','Eye Studies'].map(s => (
                     <span class="px-3 py-2 bg-white/5 border border-white/10 rounded-xl text-[11px] font-bold text-gray-400 hover:text-white hover:border-amber-500/40 hover:bg-amber-500/10 transition-all cursor-default">{s}</span>
                   ))}
                 </div>
               </div>
            </div>
          </div>
        </div>
      </section>

    </div>

      
<!-- CTA -->
      <section id="contact" class="py-40 px-4 md:px-6">
        <div class="max-w-5xl mx-auto text-center">
          <div data-reveal class="mb-6">
            <span class="text-xs font-black text-gray-600 uppercase tracking-[0.3em]">Enough talking.</span>
          </div>
          <h2 data-word-reveal class="text-[clamp(3rem,9vw,8rem)] font-black tracking-[-0.04em] leading-none mb-12">
            {["LET'S","SHIP","SOMETHING"].map((w,i) => (
              <span class="reveal-word block" style={`transition-delay:${i*100}ms`}>
                <span class={i===1 ? 'text-shimmer' : 'text-white'}>{w}</span>
              </span>
            ))}
            <span class="reveal-word block" style="transition-delay:300ms"><span class="text-white">REAL</span><span class="text-violet-500">.</span></span>
          </h2>
          <div data-reveal class="flex flex-col sm:flex-row items-center justify-center gap-5">
            <a href="mailto:lapl1412.work@gmail.com" class="magnetic group relative px-10 py-5 rounded-full hero-cta-primary font-black uppercase tracking-widest overflow-hidden hover:shadow-[0_0_80px_rgba(139,92,246,0.5)] transition-all duration-500">
              <span class="relative z-10">Say Hello</span>
              <div class="absolute inset-0 bg-gradient-to-r from-violet-600 to-blue-600 opacity-0 group-hover:opacity-100 transition-opacity duration-500"></div>
              <span class="absolute inset-0 z-10 flex items-center justify-center text-white font-black uppercase tracking-widest opacity-0 group-hover:opacity-100 transition-opacity duration-500">Say Hello</span>
            </a>
            <a href="https://www.linkedin.com/in/trung-l%E1%BA%ADp-luci-ba0b6a339/" target="_blank" class="magnetic text-sm font-bold text-gray-500 hover:text-white transition-colors duration-300 uppercase tracking-widest flex items-center gap-2">LinkedIn <span>↗</span></a>
          </div>
        </div>
      </section>

      <!-- FOOTER -->
      <footer class="py-16 border-t border-white/5 px-4 md:px-6">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row items-center justify-between gap-8">
          <a href="/" class="flex items-center gap-3 opacity-80 hover:opacity-100 transition-opacity">
            <img src="/images/logo_icon.png" alt="Luci Studio Logo" class="h-8 w-8 rounded-lg" />
            <span class="text-2xl font-black tracking-tighter">LUCI-STUDIO<span class="text-violet-500">.</span></span>
          </a>
          <div class="flex items-center gap-8">
            {[['GitHub','https://github.com/kido-luci'],['LinkedIn','https://www.linkedin.com/in/trung-l%E1%BA%ADp-luci-ba0b6a339/'],['Medium','https://medium.com/@lapl1412.work']].map(([l,h]) => (
              <a href={h} target="_blank" class="text-xs font-bold text-gray-600 hover:text-white transition-colors uppercase tracking-widest">{l}</a>
            ))}
          </div>
          <div class="flex flex-col items-center md:items-end gap-3">
            <div class="flex flex-col items-center md:items-end gap-1">
              <a href="mailto:lapl1412.work@gmail.com" class="text-xs font-bold text-gray-500 hover:text-white transition-colors uppercase tracking-widest">lapl1412.work@gmail.com</a>
              <a href="tel:+84363050702" class="text-xs font-bold text-gray-500 hover:text-white transition-colors uppercase tracking-widest">+84 363 050 702</a>
            </div>
            <p class="text-xs text-gray-700 uppercase tracking-widest">© 2026 Luci - Trung Lap · Ho Chi Minh City</p>
          </div>
        </div>
      </footer>

  </main>

</Layout>

<style>
@keyframes fadeSlideUp {
  from { opacity: 0; transform: translateY(30px); }
  to { opacity: 1; transform: translateY(0); }
}
@keyframes float {
  0%, 100% { transform: translateY(0); }
  50% { transform: translateY(-24px); }
}
@keyframes pulse-dot {
  0%, 100% { opacity: 1; transform: scale(1); }
  50% { opacity: 0.5; transform: scale(0.7); }
}
@keyframes orb-float-1 {
  0%, 100% { transform: translate(0, 0) scale(1); }
  33% { transform: translate(30px, -40px) scale(1.05); }
  66% { transform: translate(-20px, 20px) scale(0.97); }
}
@keyframes orb-float-2 {
  0%, 100% { transform: translate(0, 0) scale(1); }
  40% { transform: translate(-35px, 30px) scale(1.08); }
  70% { transform: translate(25px, -25px) scale(0.95); }
}
@keyframes divider-expand {
  from { width: 0; opacity: 0; }
  to { width: 100%; opacity: 1; }
}
@keyframes diamond-spin {
  0%  { transform: rotate(0deg) scale(1); }
  50% { transform: rotate(180deg) scale(1.3); }
  100%{ transform: rotate(360deg) scale(1); }
}

/* ── Semantic CTA button classes (theme-aware) ── */
.hero-cta-primary {
  background: white;
  color: black;
}
body.light-mode .hero-cta-primary {
  background: rgb(17, 24, 39);
  color: white;
}
.hero-cta-secondary {
  border-color: rgba(255, 255, 255, 0.1);
  color: rgb(156, 163, 175);
}
.hero-cta-secondary:hover {
  color: white;
  border-color: rgba(255, 255, 255, 0.3);
  background: rgba(255, 255, 255, 0.05);
}
body.light-mode .hero-cta-secondary {
  border-color: rgba(0, 0, 0, 0.12);
  color: rgb(107, 114, 128);
}
body.light-mode .hero-cta-secondary:hover {
  color: rgb(17, 24, 39);
  border-color: rgba(0, 0, 0, 0.25);
  background: rgba(0, 0, 0, 0.04);
}

/* ── Light Mode Overrides ── */
body.light-mode nav {
  border-bottom-color: rgba(0, 0, 0, 0.08) !important;
}

/* Nav links */
body.light-mode nav a {
  color: rgb(75, 85, 99);
}
body.light-mode nav a:hover {
  color: rgb(17, 24, 39);
}
body.light-mode nav .text-xl {
  color: rgb(17, 24, 39);
}

/* Theme toggle button */
body.light-mode #theme-toggle {
  border-color: rgba(0, 0, 0, 0.1);
}
body.light-mode #theme-toggle svg {
  color: rgb(107, 114, 128);
}
body.light-mode #theme-toggle:hover svg {
  color: rgb(17, 24, 39);
}

/* "Let's Talk" button in nav */
body.light-mode nav .magnetic {
  background: rgb(17, 24, 39);
  color: white;
}
body.light-mode nav .magnetic:hover {
  background: #8b5cf6;
}

/* Hero text */
body.light-mode .hero-line-top .text-white,
body.light-mode .hero-line-bottom .text-white {
  color: rgb(17, 24, 39) !important;
}
body.light-mode .hero-line-bottom .text-gray-300 {
  color: rgb(107, 114, 128) !important;
}

body.light-mode .hero-badge {
  border-color: rgba(139, 92, 246, 0.3);
  background: rgba(139, 92, 246, 0.08);
}
body.light-mode .hero-badge-text {
  color: rgb(75, 85, 99);
}
body.light-mode .hero-subtitle {
  color: rgb(75, 85, 99);
}
body.light-mode .hero-stat-pill {
  color: rgb(107, 114, 128);
  border-color: rgba(0, 0, 0, 0.08);
  background: rgba(0, 0, 0, 0.02);
}
body.light-mode .hero-stat-pill:hover {
  color: rgb(17, 24, 39);
  border-color: rgba(139, 92, 246, 0.3);
}
body.light-mode .hero-stat-sep {
  color: rgba(0, 0, 0, 0.15);
}

/* CTA buttons in hero */
body.light-mode .hero-line ~ div a.magnetic:first-child,
body.light-mode section a[href="#work"].magnetic {
  background: rgb(17, 24, 39);
  color: white;
}
body.light-mode section a[href="#art"].magnetic {
  border-color: rgba(0, 0, 0, 0.12);
  color: rgb(107, 114, 128);
}
body.light-mode section a[href="#art"].magnetic:hover {
  color: rgb(17, 24, 39);
  border-color: rgba(0, 0, 0, 0.3);
  background: rgba(0, 0, 0, 0.04);
}

/* Scroll hint */
body.light-mode .absolute.bottom-10 span {
  color: rgb(107, 114, 128);
}

/* Marquee strip */
body.light-mode .border-y {
  border-color: rgba(0, 0, 0, 0.06) !important;
  background: rgba(0, 0, 0, 0.02) !important;
}
body.light-mode .animate-marquee .text-gray-400 {
  color: rgb(107, 114, 128) !important;
}
body.light-mode .animate-marquee .text-gray-700 {
  color: rgb(209, 213, 219) !important;
}

/* Stats section */
body.light-mode .border-b {
  border-color: rgba(0, 0, 0, 0.06) !important;
}
body.light-mode .tilt-card {
  background-color: rgba(255, 255, 255, 0.85) !important;
  border-color: rgba(0, 0, 0, 0.07) !important;
  box-shadow: 0 2px 16px rgba(0, 0, 0, 0.06);
}
body.light-mode .tilt-card:hover {
  border-color: rgba(139, 92, 246, 0.25) !important;
  box-shadow: 0 8px 32px rgba(139, 92, 246, 0.1);
}
/* Stats number gradient */
body.light-mode .bg-gradient-to-br.from-white.to-gray-500 {
  background-image: linear-gradient(to bottom right, rgb(17, 24, 39), rgb(107, 114, 128)) !important;
}
body.light-mode .tilt-card .text-white {
  color: rgb(17, 24, 39) !important;
}
body.light-mode .tilt-card .text-gray-400 {
  color: rgb(107, 114, 128) !important;
}

/* Divider lines */
body.light-mode .opacity-20 .bg-gradient-to-b {
  background-image: linear-gradient(to bottom, rgba(0,0,0,0.15), transparent) !important;
}

/* Content wrapper */
body.light-mode .relative.bg-black\/60 {
  background-color: rgba(248, 248, 252, 0.15) !important;
  border-color: rgba(0, 0, 0, 0.06) !important;
}

/* About section */
body.light-mode .border-white\/10 {
  border-color: rgba(0, 0, 0, 0.08) !important;
}
body.light-mode .bg-white\/\[0\.02\] {
  background-color: rgba(255, 255, 255, 0.9) !important;
}
body.light-mode .bg-black\/40 {
  background-color: rgba(255, 255, 255, 0.85) !important;
}
body.light-mode .text-gray-400 {
  color: rgb(75, 85, 99) !important;
}
body.light-mode .text-gray-300 {
  color: rgb(55, 65, 81) !important;
}
body.light-mode .text-gray-500 {
  color: rgb(107, 114, 128) !important;
}
body.light-mode .text-gray-600 {
  color: rgb(107, 114, 128) !important;
}
body.light-mode .text-white {
  color: rgb(17, 24, 39) !important;
}
/* Keep accent colors vivid — must come AFTER the text-white override */
body.light-mode .text-violet-500 { color: #8b5cf6 !important; }
body.light-mode .text-violet-400 { color: #a78bfa !important; }
body.light-mode .text-pink-500   { color: #ec4899 !important; }
body.light-mode .text-blue-500   { color: #3b82f6 !important; }
body.light-mode .text-emerald-500{ color: #10b981 !important; }
body.light-mode .text-amber-500  { color: #f59e0b !important; }
body.light-mode .text-cyan-500   { color: #06b6d4 !important; }

/* Quick facts grid */
body.light-mode .border-white\/5 {
  border-color: rgba(0, 0, 0, 0.06) !important;
}
body.light-mode .hover\:bg-white\/\[0\.05\]:hover {
  background-color: rgba(139, 92, 246, 0.05) !important;
}

/* Blog cards */
body.light-mode article.tilt-card h3 {
  color: rgb(17, 24, 39) !important;
}
body.light-mode .tile-like-count {
  color: rgb(107, 114, 128) !important;
}
body.light-mode .text-gray-700 {
  color: rgb(156, 163, 175) !important;
}

/* Experience section dividers */
body.light-mode .bg-white\/5 {
  background-color: rgba(0, 0, 0, 0.06) !important;
}

/* Skills tag pills */
body.light-mode .bg-white\/5.border-white\/10 {
  background-color: rgba(0, 0, 0, 0.03) !important;
  border-color: rgba(0, 0, 0, 0.08) !important;
  color: rgb(75, 85, 99) !important;
}

/* Now section list items */
body.light-mode .text-amber-500 {
  color: #d97706 !important;
}

/* CTA section */
body.light-mode #contact h2 .text-white {
  color: rgb(17, 24, 39) !important;
}
body.light-mode #contact a[href^="mailto"] {
  background: rgb(17, 24, 39);
  color: white;
}
body.light-mode #contact a[href^="mailto"]:hover {
  box-shadow: 0 0 80px rgba(139, 92, 246, 0.4);
}

/* Footer */
body.light-mode footer {
  border-color: rgba(0, 0, 0, 0.06) !important;
}
body.light-mode footer .text-gray-600 {
  color: rgb(107, 114, 128) !important;
}
body.light-mode footer .text-gray-700 {
  color: rgb(156, 163, 175) !important;
}
body.light-mode footer .text-gray-500 {
  color: rgb(107, 114, 128) !important;
}
body.light-mode footer a:hover {
  color: rgb(17, 24, 39) !important;
}
body.light-mode footer span.text-xl {
  color: rgb(17, 24, 39) !important;
}

/* Timeline active state */
body.light-mode .timeline-item.active {
  background: linear-gradient(135deg, rgba(204, 95, 150, 0.12) 0%, rgba(255, 255, 255, 1) 40%, rgba(255, 255, 255, 1) 100%) !important;
  border-color: rgba(236, 72, 153, 0.4) !important;
  box-shadow: 0 20px 40px -20px rgba(236, 72, 153, 0.15);
}

/* Mobile menu */
body.light-mode #mobile-menu {
  background: rgba(255, 255, 255, 0.98) !important;
  backdrop-filter: blur(24px);
}
body.light-mode .mobile-nav-link {
  color: rgb(17, 24, 39) !important;
}
body.light-mode .mobile-nav-link:hover {
  color: #8b5cf6 !important;
}
body.light-mode #mobile-menu .h-px {
  background-color: rgba(0, 0, 0, 0.08) !important;
}
body.light-mode #mobile-menu a[href="#contact"] {
  background: rgb(17, 24, 39) !important;
  color: white !important;
}

/* ── Floating orbs ── */
.hero-orb {
  position: absolute;
  border-radius: 50%;
  pointer-events: none;
  filter: blur(80px);
  z-index: 0;
}
.hero-orb-1 {
  width: 520px; height: 520px;
  background: radial-gradient(circle, rgba(139,92,246,0.18) 0%, transparent 70%);
  top: 5%; left: -10%;
  /* animation handled by anime.js */
}
body.light-mode .hero-orb-1 {
  background: radial-gradient(circle, rgba(139,92,246,0.12) 0%, transparent 70%);
}
.hero-orb-2 {
  width: 400px; height: 400px;
  background: radial-gradient(circle, rgba(59,130,246,0.14) 0%, transparent 70%);
  bottom: 10%; right: -8%;
  /* animation handled by anime.js */
}
body.light-mode .hero-orb-2 {
  background: radial-gradient(circle, rgba(59,130,246,0.1) 0%, transparent 70%);
}

/* ── Role Badge ── */
.hero-badge {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 6px 18px;
  margin-bottom: 2rem;
  border: 1px solid rgba(139,92,246,0.3);
  border-radius: 999px;
  background: rgba(139,92,246,0.07);
  backdrop-filter: blur(8px);
}
.hero-badge-dot {
  width: 7px; height: 7px;
  border-radius: 50%;
  background: #22c55e;
  box-shadow: 0 0 8px rgba(34,197,94,0.8);
  animation: pulse-dot 2s ease-in-out infinite;
  flex-shrink: 0;
}
.hero-badge-text {
  font-size: 0.7rem;
  font-weight: 700;
  letter-spacing: 0.18em;
  text-transform: uppercase;
  color: rgba(200,200,200,0.8);
}

/* ── Main Headline ── */
.hero-headline {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 0;
  margin-bottom: 2rem;
  font-family: 'Outfit', sans-serif;
}
.hero-line {
  display: flex;
  align-items: baseline;
  justify-content: center;
  flex-wrap: wrap;
  gap: 0.25em;
  line-height: 1.2;
}
.hero-line-top {
  font-size: clamp(3.5rem, 11vw, 9.5rem);
  font-weight: 900;
  letter-spacing: -0.005em;
}
.hero-line-bottom {
  font-size: clamp(0.85rem, 3.5vw, 3.2rem);
  font-weight: 700;
  letter-spacing: 0.1em;
  text-transform: uppercase;
  color: rgba(255,255,255,0.45);
}
.hero-word-main,
.hero-word-accent,
.hero-word-sub {
  display: inline-block;
  overflow: hidden;
  vertical-align: middle;
  padding: 0.15em 0.05em; /* Increased vertical and added horizontal padding */
  margin: -0.15em -0.05em;
}

/* ── Divider ── */
.hero-divider {
  display: flex;
  align-items: center;
  gap: 12px;
  width: min(480px, 80vw);
  margin: 0.6rem 0 0.4rem;
}
.hero-divider-line {
  flex: 1;
  height: 1px;
  background: linear-gradient(90deg, transparent, rgba(139,92,246,0.5), rgba(59,130,246,0.4), transparent);
  opacity: 0;
  animation: divider-expand 1.2s cubic-bezier(0.16,1,0.3,1) 0.8s forwards;
}
.hero-divider-diamond {
  font-size: 0.55rem;
  color: rgba(139,92,246,0.7);
  animation: diamond-spin 8s linear infinite;
  flex-shrink: 0;
}

/* ── Subtitle ── */
.hero-subtitle {
  font-size: clamp(0.88rem, 2vw, 1.15rem);
  color: rgba(156,163,175,0.85);
  max-width: 720px;
  margin: 0 auto 2rem;
  line-height: 1.75;
  padding: 0 0.5rem;
  min-height: 6.5em;
}

/* ── Stat pills ── */
.hero-stats {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-wrap: wrap;
  gap: 8px;
  margin-top: 2.5rem;
}
.hero-stat-pill {
  font-size: 0.65rem;
  font-weight: 700;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  color: rgba(156,163,175,0.6);
  padding: 4px 12px;
  border: 1px solid rgba(255,255,255,0.06);
  border-radius: 999px;
  background: rgba(255,255,255,0.02);
  transition: color 0.3s, border-color 0.3s;
}
.hero-stat-pill:hover {
  color: rgba(255,255,255,0.8);
  border-color: rgba(139,92,246,0.3);
}
.hero-stat-sep {
  color: rgba(255,255,255,0.15);
  font-size: 1rem;
}

/* ── Timeline ── */
.timeline-item.active {
  background: linear-gradient(135deg, rgba(204, 95, 150, 0.1) 0%, rgba(3, 3, 3, 1) 40%, rgba(3, 3, 3, 1) 100%) !important;
  border-color: rgba(236, 72, 153, 0.4) !important;
  box-shadow: 0 20px 40px -20px rgba(236, 72, 153, 0.2);
}

/* ── Mobile overrides (< 480px) ── */
@media (max-width: 480px) {
  .hero-badge {
    margin-bottom: 1.2rem;
  }
  .hero-headline {
    margin-bottom: 1.2rem;
  }
  .hero-line {
    /* Stack each word on its own line on very small screens */
    flex-direction: column;
    align-items: center;
    gap: 0.05em;
  }
  .hero-line-top {
    /* Each word becomes its own full-width line */
    font-size: clamp(2.6rem, 18vw, 4.5rem);
    letter-spacing: -0.03em;
  }
  .hero-line-bottom {
    font-size: clamp(1.8rem, 12vw, 3rem);
    letter-spacing: -0.02em;
  }
  .hero-divider {
    width: 70vw;
    margin: 0.4rem 0 0.2rem;
  }
  .hero-subtitle {
    font-size: 0.88rem;
    margin-bottom: 1.5rem;
    line-height: 1.65;
  }
  .hero-stats {
    margin-top: 1.5rem;
    gap: 6px;
  }
  .hero-stat-sep {
    display: none;
  }
}

/* ── Tablet overrides (480–768px) ── */
@media (min-width: 481px) and (max-width: 768px) {
  .hero-line-top {
    font-size: clamp(2.4rem, 9vw, 6rem);
  }
  .hero-line-bottom {
    font-size: clamp(1.6rem, 6.5vw, 4rem);
  }
}

/* ── Pinterest-style masonry for art section ── */
.masonry-art-grid {
  columns: 2;
  column-gap: 0.75rem;
}
@media (min-width: 640px) {
  .masonry-art-grid {
    columns: 3;
    column-gap: 1rem;
  }
}
@media (min-width: 1024px) {
  .masonry-art-grid {
    columns: 4;
    column-gap: 1rem;
  }
}
.masonry-art-item {
  break-inside: avoid;
  margin-bottom: 0.75rem;
  display: block;
}
@media (min-width: 640px) {
  .masonry-art-item {
    margin-bottom: 1rem;
  }
}
/* Art image frame border */
.art-img-frame {
  border-color: rgba(168, 85, 247, 0.35);
}
body.light-mode .art-img-frame {
  border-color: rgba(168, 85, 247, 0.25);
}

/* ── Blog card scroll-triggered reveal ── */
.blog-card {
  opacity: 0;
  transform: translateY(28px);
}
.blog-card.card-visible {
  animation: fadeSlideUp 0.6s cubic-bezier(0.16, 1, 0.3, 1) both;
}
/* Glow on hover — violet shadow lift */
.blog-card:hover {
  box-shadow: 0 10px 40px rgba(139, 92, 246, 0.18),
              0 0 0 1px rgba(139, 92, 246, 0.15);
  border-color: rgba(139, 92, 246, 0.3) !important;
}

/* ── Art item scroll-triggered reveal ── */
@keyframes artFadeIn {
  from { opacity: 0; transform: scale(0.88) translateY(24px); }
  to   { opacity: 1; transform: scale(1)    translateY(0); }
}
[data-art-card] {
  opacity: 0;
}
[data-art-card].card-visible {
  animation: artFadeIn 0.55s cubic-bezier(0.16, 1, 0.3, 1) both;
}
/* Art item glow on hover */
[data-art-card]:hover .art-img-frame {
  box-shadow: 0 8px 32px rgba(168, 85, 247, 0.25);
}

/* ── Blog card image placeholder (shown while loading or on error) ── */
.blog-img-container {
  background: linear-gradient(135deg, rgba(139, 92, 246, 0.15) 0%, rgba(59, 130, 246, 0.10) 100%);
}
body.light-mode .blog-img-container {
  background: linear-gradient(135deg, rgba(139, 92, 246, 0.08) 0%, rgba(59, 130, 246, 0.05) 100%);
}
@media (max-width: 767px) {
  #art-bg {
    background-image: url('https://media.luci-studio.com/ChatGPT%20Image%20May%203%2C%202026%2C%2009_10_36%20AM.png') !important;
  }
}

.hero-bg-img {
  background-image: url('https://media.luci-studio.com/58df4b137ab3844249ec8241269baf14.jpg');
  mix-blend-mode: screen;
}
@media (orientation: landscape) {
  .hero-bg-img {
    background-image: url('https://media.luci-studio.com/ChatGPT%20Image%20May%203%2C%202026%2C%2010_55_42%20AM.jpg');
  }
}
body.light-mode .hero-bg-img {
  mix-blend-mode: multiply;
}
</style>

<script>
  // Mobile Menu Logic
  const toggle = document.getElementById('mobile-menu-toggle');
  const menu = document.getElementById('mobile-menu');
  const links = document.querySelectorAll('.mobile-nav-link');

  toggle?.addEventListener('click', () => {
    toggle.classList.toggle('active');
    menu?.classList.toggle('opacity-100');
    menu?.classList.toggle('pointer-events-auto');
    // Toggle body scroll
    document.body.style.overflow = toggle.classList.contains('active') ? 'hidden' : '';
  });

  links.forEach(link => {
    link.addEventListener('click', () => {
      toggle?.classList.remove('active');
      menu?.classList.remove('opacity-100');
      menu?.classList.remove('pointer-events-auto');
      document.body.style.overflow = '';
    });
  });

  // Auto-close menu on landscape or larger screens
  window.addEventListener('resize', () => {
    if (window.innerWidth >= 768 || window.matchMedia("(orientation: landscape)").matches) {
      if (toggle?.classList.contains('active')) {
        toggle.classList.remove('active');
        menu?.classList.remove('opacity-100', 'pointer-events-auto');
        document.body.style.overflow = '';
      }
    }
  });


  // Blog card scroll-triggered stagger reveal
  // Negative rootMargin ensures cards animate only when entering visible viewport, not while off-screen
  (function () {
    const cards = Array.from(document.querySelectorAll<HTMLElement>('[data-blog-card]'));
    if (!cards.length) return;
    const obs = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        const el = entry.target as HTMLElement;
        const idx = parseInt(el.dataset.blogCard ?? '0', 10);
        // Stagger within each row of 3 columns; cap at 0.24s max
        el.style.animationDelay = `${(idx % 3) * 0.12}s`;
        el.classList.add('card-visible');
        obs.unobserve(el);
      });
    }, { threshold: 0.12, rootMargin: '0px 0px -20px 0px' });
    cards.forEach(c => obs.observe(c));
  })();

  // Art item scroll-triggered stagger reveal
  (function () {
    const items = Array.from(document.querySelectorAll<HTMLElement>('[data-art-card]'));
    if (!items.length) return;
    const obs = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        const el = entry.target as HTMLElement;
        const idx = items.indexOf(el);
        el.style.animationDelay = `${(idx % 4) * 0.09}s`;
        el.classList.add('card-visible');
        obs.unobserve(el);
      });
    }, { threshold: 0.05, rootMargin: '0px 0px -20px 0px' });
    items.forEach(i => obs.observe(i));
  })();

  // Blog tile like buttons
  (function() {
    const API_URL = (document.querySelector('main[data-api-url]') as HTMLElement | null)?.dataset.apiUrl || '';

    function burstHearts(anchor: HTMLElement) {
      const count = 6;
      const rect = anchor.getBoundingClientRect();
      for (let i = 0; i < count; i++) {
        const h = document.createElement('span');
        h.textContent = '♥';
        const angle = (i / count) * 360;
        const dist = 28 + Math.random() * 20;
        const dx = Math.cos((angle * Math.PI) / 180) * dist;
        const dy = Math.sin((angle * Math.PI) / 180) * dist - 18;
        h.style.cssText = `
          position:fixed;
          left:${rect.left + rect.width / 2}px;
          top:${rect.top + rect.height / 2}px;
          font-size:${9 + Math.random() * 7}px;
          color:#f43f5e;
          pointer-events:none;
          z-index:9999;
          transform:translate(-50%,-50%);
          transition:transform 0.6s ease-out,opacity 0.6s ease-out;
          will-change:transform,opacity;
        `;
        document.body.appendChild(h);
        requestAnimationFrame(() => {
          h.style.transform = `translate(calc(-50% + ${dx}px), calc(-50% + ${dy}px)) scale(0)`;
          h.style.opacity = '0';
        });
        setTimeout(() => h.remove(), 650);
      }
    }

    function pulseIcon(icon: HTMLElement) {
      icon.style.transform = 'scale(1.6)';
      icon.style.transition = 'transform 0.15s ease-out';
      setTimeout(() => {
        icon.style.transform = 'scale(1)';
        icon.style.transition = 'transform 0.2s ease-in';
      }, 150);
    }

    document.querySelectorAll('.tile-like-area').forEach(el => {
      const btn = el as HTMLElement;
      const postId = btn.dataset.id;
      const icon = btn.querySelector('.tile-like-icon') as HTMLElement | null;
      const countEl = btn.querySelector('.tile-like-count') as HTMLElement | null;
      const likedKey = `liked_${postId}`;
      let liked = localStorage.getItem(likedKey) === '1';

      function applyLikedState(state: boolean) {
        liked = state;
        if (state) {
          icon?.setAttribute('fill', '#f43f5e');
          icon?.setAttribute('stroke', '#f43f5e');
          countEl?.classList.add('text-rose-400');
          countEl?.classList.remove('text-gray-500');
        } else {
          icon?.setAttribute('fill', 'none');
          icon?.setAttribute('stroke', 'currentColor');
          countEl?.classList.remove('text-rose-400');
          countEl?.classList.add('text-gray-500');
        }
      }

      applyLikedState(liked);

      btn.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        const isLiking = !liked;
        if (isLiking) {
          burstHearts(btn);
          if (icon) pulseIcon(icon);
        }
        const endpoint = liked ? 'unlike' : 'like';
        fetch(`${API_URL}/posts/${postId}/${endpoint}`, { method: 'POST' })
          .then(r => r.json())
          .then(d => {
            if (d.likes != null && countEl) countEl.textContent = d.likes;
            applyLikedState(!liked);
          })
          .catch(() => {});
      });
    });
  })();

  // Art tile like buttons
  (function() {
    const API_URL = (document.querySelector('main[data-api-url]') as HTMLElement | null)?.dataset.apiUrl || '';

    document.querySelectorAll('.art-like-area').forEach(el => {
      const btn = el as HTMLElement;
      const postId = btn.dataset.id;
      const icon = btn.querySelector('.art-like-icon') as HTMLElement | null;
      const countEl = btn.querySelector('.art-like-count') as HTMLElement | null;
      const likedKey = `liked_${postId}`;
      let liked = localStorage.getItem(likedKey) === '1';

      function apply(state: boolean) {
        liked = state;
        if (state) {
          icon?.setAttribute('fill', '#f43f5e');
          icon?.setAttribute('stroke', '#f43f5e');
          countEl?.classList.add('text-rose-400');
          countEl?.classList.remove('text-white/60');
        } else {
          icon?.setAttribute('fill', 'none');
          icon?.setAttribute('stroke', 'currentColor');
          countEl?.classList.remove('text-rose-400');
          countEl?.classList.add('text-gray-500');
        }
      }

      apply(liked);

      btn.addEventListener('click', (e) => {
        e.preventDefault();
        e.stopPropagation();
        const endpoint = liked ? 'unlike' : 'like';
        icon && (icon.style.transform = 'scale(1.5)');
        setTimeout(() => { if (icon) icon.style.transform = 'scale(1)'; }, 150);
        fetch(`${API_URL}/posts/${postId}/${endpoint}`, { method: 'POST' })
          .then(r => r.json())
          .then(d => {
            if (d.likes != null && countEl) countEl.textContent = d.likes;
            apply(!liked);
          })
          .catch(() => {});
      });
    });
  })();

  // ML6 "Beautiful Questions" one-shot letter animation
  // Word-level wrappers (white-space:nowrap) keep line-breaks between words, not mid-word
  (function() {
    const el = document.querySelector('.hero-subtitle') as HTMLElement | null;
    if (!el) return;
    const text = el.textContent?.trim() ?? '';
    const trans = 'opacity 0.75s cubic-bezier(0.19,1,0.22,1),transform 0.75s cubic-bezier(0.19,1,0.22,1)';
    let idx = 0;
    el.innerHTML = text.split(' ').map(word =>
      `<span style="white-space:nowrap;display:inline">${
        [...word].map(ch =>
          `<span style="display:inline-block;opacity:0;transform:translateY(1.2em);transition:${trans};transition-delay:${300 + idx++ * 12}ms">${ch}</span>`
        ).join('')
      }</span>`
    ).join(' ');
    requestAnimationFrame(() => requestAnimationFrame(() => {
      el.querySelectorAll<HTMLElement>('span span').forEach(s => {
        s.style.opacity = '1';
        s.style.transform = 'translateY(0)';
      });
    }));
  })();


  // Theme Toggle Icon Update
  const updateThemeIcon = () => {
    const isLight = document.body.classList.contains('light-mode');
    const darkIcon = document.querySelector('.dark-icon');
    const lightIcon = document.querySelector('.light-icon');
    
    if (isLight) {
      darkIcon?.classList.add('hidden');
      lightIcon?.classList.remove('hidden');
    } else {
      darkIcon?.classList.remove('hidden');
      lightIcon?.classList.add('hidden');
    }
  };

  // Update icon on page load
  updateThemeIcon();

  // Listen for theme changes
  const themeToggleBtn = document.getElementById('theme-toggle');
  themeToggleBtn?.addEventListener('click', () => {
    setTimeout(updateThemeIcon, 50);
  });
</script>

<script>
  // ML9 "Coffee Mornings" — scale from 0→1 with spring bounce, triggered on scroll into view
  // All styles inline to bypass Astro scoped CSS (dynamic spans have no scope attribute)
  (function () {
    // CSS equivalent of anime.js elasticity:600 — cubic-bezier with overshoot
    const TRANS = 'transform 0.8s cubic-bezier(0.34,1.56,0.64,1)';
    const INIT  = `display:inline-block;transform:scale(0);transform-origin:50% 100%;transition:${TRANS}`;

    function initLetters(h2: HTMLElement): HTMLElement[] {
      const letters: HTMLElement[] = [];
      const nodes = Array.from(h2.childNodes);
      h2.innerHTML = '';

      for (const node of nodes) {
        if (node.nodeType === Node.TEXT_NODE) {
          for (const ch of node.textContent ?? '') {
            if (ch === ' ') { h2.appendChild(document.createTextNode(' ')); continue; }
            const s = document.createElement('span');
            s.setAttribute('style', INIT);
            s.textContent = ch;
            h2.appendChild(s);
            letters.push(s);
          }
        } else if (node.nodeType === Node.ELEMENT_NODE) {
          const orig = node as HTMLElement;
          const wrap = orig.cloneNode(false) as HTMLElement;
          for (const ch of orig.textContent ?? '') {
            const s = document.createElement('span');
            s.setAttribute('style', INIT);
            s.textContent = ch;
            wrap.appendChild(s);
            letters.push(s);
          }
          h2.appendChild(wrap);
        }
      }
      return letters;
    }

    const letterMap = new Map<Element, HTMLElement[]>();
    document.querySelectorAll<HTMLElement>('[data-ml9]').forEach(h2 => {
      letterMap.set(h2, initLetters(h2));
    });

    const observer = new IntersectionObserver(entries => {
      entries.forEach(entry => {
        if (!entry.isIntersecting) return;
        observer.unobserve(entry.target);
        const letters = letterMap.get(entry.target) ?? [];
        letters.forEach((s, i) => {
          s.style.transitionDelay = `${(i + 1) * 45}ms`;
          s.style.transform = 'scale(1)';
        });
      });
    }, { threshold: 0.3 });

    document.querySelectorAll('[data-ml9]').forEach(el => observer.observe(el));
  })();



  // Pause infinite CSS animations when their host element is off-screen
  (() => {
    const pauseWhenHidden = (selector: string, root: Element | null = null) => {
      const els = document.querySelectorAll<HTMLElement>(selector);
      if (!els.length) return;
      const io = new IntersectionObserver(entries => {
        entries.forEach(e => {
          (e.target as HTMLElement).style.animationPlayState = e.isIntersecting ? 'running' : 'paused';
        });
      }, { rootMargin: '100px', threshold: 0 });
      els.forEach(el => { el.style.animationPlayState = 'paused'; io.observe(el); });
    };

    pauseWhenHidden('.hero-badge-dot');
    pauseWhenHidden('.hero-divider-diamond');
    pauseWhenHidden('.animate-marquee');
    // Scroll-hint float line
    document.querySelectorAll<HTMLElement>('[style*="animation:float"]').forEach(el => {
      const io = new IntersectionObserver(entries => {
        entries.forEach(e => { (e.target as HTMLElement).style.animationPlayState = e.isIntersecting ? 'running' : 'paused'; });
      }, { threshold: 0 });
      io.observe(el);
    });
  })();
</script>

