---
// Thin route wrapper — the homepage body lives in HomePage so page logic stays
// out of the route file. English-only: only /blog is mirrored under /vi (see
// isBilingualPath in src/i18n).
import HomePage from "../components/pages/HomePage.astro";
---
<HomePage />
