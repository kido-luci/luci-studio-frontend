---
// Thin route wrapper — the homepage body lives in HomePage so / (en) and /vi/
// render the same component, which reads the locale from the URL.
import HomePage from "../components/pages/HomePage.astro";
---
<HomePage />
