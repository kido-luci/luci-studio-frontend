---
// Thin route wrapper — shared body lives in PrivacyPage so /privacy (en) and
// /vi/privacy render the same component, which reads the locale from the URL.
import PrivacyPage from "../components/pages/PrivacyPage.astro";
---
<PrivacyPage />
