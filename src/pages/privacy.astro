---
// Thin route wrapper — the body lives in PrivacyPage so page logic stays out of
// the route file. English-only: only /blog is mirrored under /vi (see
// isBilingualPath in src/i18n).
import PrivacyPage from "../components/pages/PrivacyPage.astro";
---
<PrivacyPage />
